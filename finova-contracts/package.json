{
  "name": "finova-network-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "api",
    "client/typescript"
  ],
  "scripts": {
    "test": "yarn workspaces run test",
    "build": "yarn workspaces run build",
    "lint": "yarn workspaces run lint",
    "start:api": "yarn workspace api start"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "ts-node": "^10.9.1",
    "ts-mocha": "^10.0.0",
    "eslint": "^8.40.0",
    "prettier": "^2.8.8",
    "lerna": "^6.6.2"
  }
}
